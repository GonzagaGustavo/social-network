# Social Network

## Instalar pnpm
`npm install -g pnpm`

## Instalar yarn
`npm install -g yarn`

- Comando npm já vem com nodejs

## Rodar backend
`pnpm install`<br />
<br />
`pnpm dev`

## Rodar frontend
`pnpm install`<br />
<br />
`pnpm dev`

## Rodar mobile
`yarn`<br />
<br />
`yarn start`
